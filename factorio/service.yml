---
    apiVersion: v1
    kind: Service
    metadata:
      name: web
      labels:
        name: web
        app: "factorio-server"
        env: "production"
    spec:
      type: NodePort
      ports:
      - port: 34197
        name: udp
        nodePort: 30565
        protocol: UDP
      selector:
        name: web
        app: "factorio-server"
        env: "production"
